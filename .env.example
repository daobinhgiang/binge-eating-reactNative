# Firebase Configuration Example
# Copy this file to .env and replace the placeholder values with your actual Firebase project configuration
# Get these values from: Firebase Console > Project Settings > General > Your apps

# Firebase Web App Configuration
EXPO_PUBLIC_FIREBASE_API_KEY=your_api_key_here
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
EXPO_PUBLIC_FIREBASE_PROJECT_ID=your_project_id_here
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id_here
EXPO_PUBLIC_FIREBASE_APP_ID=your_app_id_here
EXPO_PUBLIC_FIREBASE_MEASUREMENT_ID=your_measurement_id_here

# Setup Instructions:
# 1. Create a Firebase project at https://console.firebase.google.com/
# 2. Add your app to the Firebase project
# 3. Copy the config values from Firebase Console > Project Settings > General > Your apps
# 4. Copy this file to .env and replace the placeholder values
# 5. Ensure .env is in your .gitignore file (already configured)

# Note: All environment variables must be prefixed with EXPO_PUBLIC_ to be accessible in the app
# Never commit .env files to version control - they contain sensitive configuration data
